version: '3.7'
services:

  api:
    build:
      context: ./api
    restart: unless-stopped
    environment:
      - PORT=${API_PORT}
      - VC_REGISTRY=${VC_REGISTRY}
      - PUBLIC_PATH=${FRONTEND_PUBLIC_PATH}
    ports:
      - "${API_PORT}:${API_PORT}"
    labels:
    - "traefik.enable=true"
    - "traefik.docker.network=ssi-suite"
    - "traefik.http.routers.dashboard.rule=PathPrefix(`/api/verify/`)"

  frontend:
    build:
      context: ./frontend
      args:
        - PUBLIC_PATH=${FRONTEND_PUBLIC_PATH}
        - NODE_ENV=${NODE_ENV}
        - BASE_URL=${BASE_URL}
    ports:
      - "${FRONTEND_PORT}:80"
    restart: unless-stopped
    labels:
    - "traefik.enable=true"
    - "traefik.docker.network=ssi-suite"
    - "traefik.http.routers.dashboard.rule=PathPrefix(`/verifier/`)"

networks:
  ssi-suite:
    external: true